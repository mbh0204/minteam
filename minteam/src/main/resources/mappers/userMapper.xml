<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ds.mappers.userMapper">

	<select id="login" resultType="com.ds.domain.UserVO">
		select user_id, user_pw, user_name
		from tbl_user
		where user_id = #{user_id}
		and user_pw = #{user_pw}
	</select>
	
	<update id="keepLogin">
		update tbl_user set
			sessionKey = #{sessionKey},
			sessionlimit = #{sessionlimit}
		where userid = #{userid}
	</update>
	
	<select id="checkUserWithSessionKey" resultType="com.ds.domain.UserVO">
		select * from tbl_user
		where sessionkey = #{sessionkey}
	</select>

</mapper>